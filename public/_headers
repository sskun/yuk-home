# Cloudflare Pages 响应头配置
# 说明：本站为纯静态、无用户输入，且 Astro 会为逐元素动画延迟/主题色使用内联 style 属性，
# 因此 CSP 对 script/style 放开 'unsafe-inline'（否则动画与主题会失效）。
# 图片治理工具需要在浏览器内处理图片，使用 blob:URL 进行预览，因此 img-src 需包含 blob:。
# Google Fonts 用于全站字体，font-src 和 style-src 需允许 fonts.googleapis.com。
# Cloudflare Beacon 用于分析（可选），script-src 需允许 static.cloudflareinsights.com。
# 其余方向仍收紧：默认仅同源、禁止被 iframe 嵌套、禁用插件对象、限制 base 标签。
/*
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  Referrer-Policy: strict-origin-when-cross-origin
  Content-Security-Policy: default-src 'self'; img-src 'self' data: https: blob:; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; script-src 'self' 'unsafe-inline' https://static.cloudflareinsights.com; font-src 'self' https://fonts.gstatic.com; object-src 'none'; base-uri 'self'; frame-ancestors 'none'
