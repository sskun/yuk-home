# Cloudflare Pages 响应头配置
# 说明：本站为纯静态、无用户输入，且 Astro 会为逐元素动画延迟/主题色使用内联 style 属性，
# 因此 CSP 对 script/style 放开 'unsafe-inline'（否则动画与主题会失效）。
# 其余方向仍收紧：默认仅同源、禁止被 iframe 嵌套、禁用插件对象、限制 base 标签。
/*
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  Referrer-Policy: strict-origin-when-cross-origin
  Content-Security-Policy: default-src 'self'; img-src 'self' data: https:; style-src 'self' 'unsafe-inline'; script-src 'self' 'unsafe-inline'; font-src 'self'; object-src 'none'; base-uri 'self'; frame-ancestors 'none'
